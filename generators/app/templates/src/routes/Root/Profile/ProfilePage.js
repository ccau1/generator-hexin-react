/* @flow */

import React, {Component} from 'react';

export default class ProfilePage extends Component {
  static propTypes = {

  };

  render() {
    return (
      <div>
        profile page
      </div>
    );
  }
}
